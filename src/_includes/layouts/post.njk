<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/prism-tomorrow.min.css" rel="stylesheet">
    <link href="/css/blog.css" rel="stylesheet">
    <title>ivywong.dev - {{ title }}</title>
</head>
<body>
    <article class="content">
        <h1 class="post-title"><a href="{{ page.url }}">{{ title }}</a></h1>
        <div class="post-metadata">
            <div class="post-date">created {{ created | readableDate }}</div>
            {% if created != page.date %} | 
            <div class="post-date">updated {{ page.date | readableDate }}</div>
            {% endif %}
            {% if tags %} | {% endif %}
            {% for tag in tags %}
            <div class="post-tag">{{ tag }}</div>
            {% endfor %}
        </div>
        {{ content | safe }}

        {% include "layouts/footer.njk" %}
    </article>
</body>
</html>